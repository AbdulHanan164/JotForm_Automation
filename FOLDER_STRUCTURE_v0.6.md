# Folder Structure — v0.6.0

Compared to v0.5.0: **5 dead directories deleted**, **3 new files added**, several files rewritten.

```
WebHook/
│
├── app/
│   ├── main.py                      MODIFIED  v0.6: auth on /submissions + /discover,
│   │                                           lifespan logs documents_dir, version 0.6.0
│   ├── auth.py                      NEW       v0.6: FIX #1/#2 — central auth module
│   │                                           require_operator(), verify_webhook_token()
│   ├── config.py                    MODIFIED  v0.6: added operator_api_key, webhook_secret,
│   │                                           documents_dir
│   ├── logger.py                    unchanged
│   │
│   ├── routes/
│   │   ├── webhook.py               REWRITTEN v0.6: token verify, idempotency, background
│   │   │                                       Sheets, document download
│   │   ├── review.py                MODIFIED  v0.6: FIX #1 — auth on all endpoints,
│   │   │                                       new POST /review/{id}/sent endpoint
│   │   └── admin.py                 MODIFIED  v0.6: FIX #1 — auth on all endpoints,
│   │                                           new GET /admin/fieldmap/arnona/status
│   │
│   ├── pipeline/
│   │   ├── orchestrator.py          unchanged (uses section-keyed parsed format)
│   │   ├── conditional_logic.py     unchanged (ConditionalLogicEngine)
│   │   └── result.py                unchanged (PipelineResult)
│   │
│   ├── services/
│   │   ├── arnona/
│   │   │   ├── service.py           unchanged
│   │   │   ├── field_map.py         REWRITTEN v0.6: FIX #3 — loads from YAML,
│   │   │   │                                   check_field_map_status(), no CITY_HIDDEN_FIELDS
│   │   │   └── rules.py             MODIFIED  v0.6: removed _arnona_field_hidden(),
│   │   │                                       _has() now checks local_path
│   │   ├── submission_service.py    MODIFIED  v0.6: removed 3 NotImplementedError stubs,
│   │   │                                       version comment updated
│   │   ├── rules/                   DELETED   v0.1 dead code
│   │   └── integrations/            DELETED   v0.1 dead code (HubSpot/AI/Drive stubs)
│   │
│   ├── config_service/
│   │   └── loader.py                REWRITTEN v0.6: FIX #4 — parse_fields() returns
│   │                                           section-keyed dict, _section_for_field(),
│   │                                           _validate_yaml_service() raises on error
│   │
│   ├── documents/
│   │   └── downloader.py            NEW       v0.6: FIX #6 — download JotForm files,
│   │                                           _safe_filename(), FileTooLargeError,
│   │                                           _manifest.json
│   │
│   ├── review/
│   │   ├── queue.py                 unchanged
│   │   └── models.py                unchanged (ReviewStatus enum)
│   │
│   ├── integrations/
│   │   ├── jotform/
│   │   │   ├── client.py            unchanged
│   │   │   └── sync.py              unchanged
│   │   └── google_sheets/
│   │       └── client.py            unchanged
│   │
│   ├── utils/
│   │   └── hebrew.py                unchanged
│   │
│   ├── parsers/                     DELETED   v0.1 dead code
│   ├── models/                      DELETED   v0.1 dead code
│   └── outputs/                     DELETED   v0.1 dead code
│
├── config/                          committed to git
│   ├── field_maps/
│   │   └── arnona.yaml              NEW       v0.6: FIX #3 — editable JotForm field ID map
│   │                                           All entries verified: false (need real IDs)
│   ├── services/
│   │   ├── arnona.yaml              DELETED   v0.6: duplicate of Python ArnonaService
│   │   ├── electricity.yaml         unchanged (YAML-driven future service)
│   │   └── water.yaml               unchanged (YAML-driven future service)
│   └── forms/
│       └── *_synced.json            auto-generated by POST /admin/sync/{form_id}
│
├── data/                            NOT committed to git (.gitignore)
│   ├── submissions/
│   │   └── {ts}_{id}_raw.json       full technical dump
│   ├── processed/
│   │   └── {ts}_{id}_business.json  Hebrew business summary
│   ├── review_queue/
│   │   └── {submission_id}.json     mutable review state
│   ├── documents/                   NEW       v0.6: FIX #6 — downloaded files
│   │   └── {submission_id}/
│   │       ├── תעודת_זהות.jpg
│   │       ├── חוזה_שכירות.pdf
│   │       └── _manifest.json
│   └── logs/
│       └── webhook.log
│
├── tests/
│   ├── __init__.py
│   ├── conftest.py                  NEW       v0.6: fixtures, sample payload, tmp dirs
│   ├── test_auth.py                 NEW       v0.6: FIX #1 — auth on all protected routes
│   ├── test_webhook.py              NEW       v0.6: FIX #2 — webhook token verification
│   ├── test_idempotency.py          NEW       v0.6: FIX #7 — duplicate submissions
│   ├── test_conditional_logic.py    NEW       v0.6: FIX #4 — section-keyed parsed format
│   ├── test_field_map.py            NEW       v0.6: FIX #3 — YAML field map loading
│   └── test_document_download.py    NEW       v0.6: FIX #6 — document download
│
├── .env                             NOT committed  contains API keys + secrets
├── .gitignore
├── requirements.txt
├── pytest.ini                       NEW       v0.6: test config
├── ARCHITECTURE_v0.6.md             NEW       v0.6: this document's companion
├── ARCHITECTURE_REVIEW_v0.5.md      reference: senior architect review findings
├── MIGRATION_v0.5_to_v0.6.md        NEW       v0.6: step-by-step upgrade guide
└── FOLDER_STRUCTURE_v0.6.md         this file

```

---

## Files Changed at a Glance

| Status | Count | Files |
|--------|-------|-------|
| NEW | 10 | `auth.py`, `downloader.py`, `arnona.yaml` (field_maps), 6 test files, `pytest.ini` |
| REWRITTEN | 3 | `webhook.py`, `loader.py`, `field_map.py` |
| MODIFIED | 5 | `main.py`, `review.py`, `admin.py`, `rules.py`, `submission_service.py`, `config.py` |
| DELETED | 6 | `parsers/`, `models/`, `outputs/`, `services/rules/`, `services/integrations/`, `config/services/arnona.yaml` |
| Unchanged | ~15 | orchestrator, pipeline, review queue, integrations, utils |

---

## Key Invariants (Never Change These)

1. **Email never sent automatically** — only returned to operator after explicit POST /approve
2. **`.env` never committed** — contains `OPERATOR_API_KEY`, `WEBHOOK_SECRET`, `JOTFORM_API_KEY`
3. **`data/` never committed** — contains client PII (names, IDs, documents)
4. **Company name** = "מה זה קל" (not "מזכ"ל")
5. **Python service takes priority** over YAML-driven service for the same form_id
6. **Parsed format** = section-keyed dict — `{"basic": {"עיר": "..."}}` not flat `{"עיר": "..."}`
