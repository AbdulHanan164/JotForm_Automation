# ── Core (required) ───────────────────────────────────────────────────────────
# Versions match the environment the 185-test suite passes against (v0.8.2).
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22
pydantic-settings==2.13.1
pyyaml==6.0.3
jinja2==3.1.6                 # operator dashboard templates (/dashboard)

# ── Optional integrations (uncomment when ready) ──────────────────────────────

# JotForm API sync — no extra dependencies needed (uses stdlib urllib)
# Already built in: app/integrations/jotform/

# Google Sheets review dashboard
# google-auth>=2.0
# google-auth-httplib2>=0.1
# google-api-python-client>=2.0

# AI document classification (Phase 10C — Gemini Vision)
# Uses the current SDK: https://github.com/googleapis/python-genai
google-genai>=1.0

# AI document extraction (OpenAI Vision / GPT-4o)
openai>=1.0

# HubSpot CRM integration
# hubspot-api-client>=8.0

# OCR (alternative to OpenAI Vision)
# pytesseract>=0.3
# Pillow>=10.0
